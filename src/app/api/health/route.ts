import { NextResponse } from "next/server";

export function GET() {
  return NextResponse.json({
    status: "ok",
    system: "studioflow",
    version: "v0.13.2",
    release: "Purchase Insight Truth Fix",
  });
}
