import { NextResponse } from "next/server";

export function GET() {
  return NextResponse.json({
    status: "ok",
    system: "studioflow",
    version: "v0.6.0",
    release: "Economic Engine Foundation",
  });
}
